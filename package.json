{
  "name": "overlay",
  "version": "0.0.0",
  "private": true,
  "description": "Merge two animations onto one shared timeline, in the browser",
  "license": "GPL-3.0-only",
  "scripts": {
    "serve": "node test/serve.js",
    "test": "playwright test",
    "corpus": "python3 corpus/make_corpus.py",
    "fixtures": "node test/export-fixtures.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
